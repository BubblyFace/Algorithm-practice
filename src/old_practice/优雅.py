import math
num = int(input().split()[0])


def count_num(n):
    if n == 0:
        return 0
    if n < 0:
        return 0
    max_num = math.sqrt(n)

    for i in list(range(max_num)):
        i

count_num(num)